[build]
builder = "nixpacks"
buildCommand = "cd backend && npm install"

[deploy]
startCommand = "cd backend && (node database/migrate.js || echo 'Migration warning - server starting anyway') && npm start"
